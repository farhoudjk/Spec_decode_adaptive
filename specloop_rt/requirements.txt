# specloop_rt — real-vLLM harness. Install on the GPU box, on top of the
# base repo's requirements.txt (numpy/pandas/matplotlib).
pyyaml>=6.0
vllm==0.9.2          # pinned: scheduler_patch.py targets the v0.9.x v1 engine
# vllm's own metadata puts no upper bound on transformers, so an unconstrained
# install pulls whatever is newest (observed: 5.14.1) and it silently fails
# on import: vllm/transformers_utils/configs/ovis.py registers "aimv2" with
# AutoConfig, which transformers 5.x already registers itself, raising
# ValueError at import time -- vllm.v1.core.sched.scheduler.Scheduler import
# fails, scheduler_patch.py's try/except swallows it, and SpecLoopScheduler
# silently subclasses `object` instead of the real Scheduler. Pin to a
# version contemporary with vllm 0.9.2 (mid-2025) to avoid this.
transformers==4.53.3
torch>=2.4
datasets>=2.19        # HumanEval, SQuAD, CNN-DailyMail (see real_corpus.py)
huggingface_hub>=0.23  # ShareGPT file download (see real_corpus.py)
